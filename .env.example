# Set these in your local user environment. This file is NOT auto-loaded.
# Supply your own SEC contact identity; never use it as the recipient setting.
EDGAR_IDENTITY=
# SMTP credentials depend on the selected provider; app password where supported.
SMTP_USERNAME=
SMTP_PASSWORD=
# Optional: evidence-only reports work without this.
OPENAI_API_KEY=
