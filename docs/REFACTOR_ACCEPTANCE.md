# Repository refactor verification, 2026-09-05

The comparison baseline is commit `2e75971`, which preserves the completed
public-voice, original-analysis and contrast work that was present before this
refactor. The refactor comparison excludes those earlier feature changes.

## Changes

- Moved the reusable reviewed-brief renderer and its unchanged template into the
  installable `servicing_brief` package.
- Retained the historical renderer and template paths, CLI defaults and
  `tools.build_company_briefs.renderer_module()` compatibility function.
- Isolated each legacy renderer import from the canonical package and other
  imports. A per-instance reentrant lock protects temporary legacy overrides.
- Consolidated brand-registry validation into one read and one validation pass
  per load. Reused source hashes within one analysis-validation call.
- Documented application code, compatibility tools and protected local history
  in `REPOSITORY.md`. No historical source or sent artifact was deleted.

## Verification

| Check | Result |
| --- | --- |
| Offline suite | 404 tests passed. |
| Report and email comparison | 35 surfaces across PFSI, TD, RY, CM, BNS, BMO and neutral TFC are byte-identical. |
| MIME comparison | Complete encoded messages compared with a fixed generation timestamp; no transport normalization. |
| Legacy and installed-package CLI | Each route reproduces all 14 baseline files, including explicit historical combined output. |
| Packaged resources | 46 wheel resources match the candidate source; four templates and six verified brand assets load outside the checkout import path. |
| Rendered QA | 60 desktop, phone, light, dark and email fallback cases passed the contrast and overflow checks. |
| Protected local files | 4,942 source, evidence, review, sent-email and brand files retain their original hashes. |
| Validation behavior | Invalid-input, cross-call freshness, distinct archive/candidate path and legacy isolation/concurrency regressions pass. |

The PFSI comparison uses the reviewed Version 2 source packet. Its HTML and
plain-text content match the accepted sent brief. The before/after guarantee for
complete MIME applies to deterministic regenerated packages; the previously
sent original is preserved separately with its original approval metadata.

Detailed local evidence is retained under `output/repo-refactor-20260905/`:
`before/`, `after/`, `comparison.json`, `protected-before.json`, CLI manifests,
wheel checks, `qa/`, and independent review records in `review/`. Generated
artifacts and email/source history remain excluded from Git.

## Lightweight follow-up, baseline `9d2be41`

This pass removes completed-review tooling and repeated runtime work. The
55-line legacy adapter replaces its 140-line patch/restore implementation;
shared source helpers replace duplicate IR/SEC definitions. Financial and
commentary extraction reuse verified spans within one report call. Packaging
tests a fitting complete packet once and counts exact SMTP bytes without
retaining a trial-message copy. Source and image libraries load when needed.

| Measure | Before | After |
| --- | ---: | ---: |
| Executable application/tool files | 64 | 52 |
| Executable application/tool bytes | 913,320 | 826,464 |
| Executable nonblank lines | 16,351 | 15,356 |
| PFSI report plus email, median | 9.475 s | 6.100 s |
| PFSI email packaging, median | 1.339 s | 1.090 s |
| Delivery import peak working set | 60.7 MB | 35.7 MB |
| Reporting import peak working set | 56.4 MB | 31.3 MB |
| Source-package import | 218 ms | 2.5 ms |

Source-size comparisons normalize CRLF to LF in both trees. Timing uses five
fresh processes per case, the same Python environment and reviewed PFSI source
packet. End-to-end peak memory is essentially unchanged. The package wheel
grows from 253,793 to 255,144 bytes because the extraction helpers, lazy exports
and explicit missing-provider diagnostics add 4,189 source bytes. All 50 wheel
members remain. Tests and operational documentation grow to cover the new
boundaries; total tracked size and executable implementation decrease.

Clean wheel installations measure 55,284,333 bytes across 15 distributions for
the base profile and 265,464,258 bytes across 46 distributions for full SEC
monitoring. Counts exclude generated bytecode and test dependencies. The 79.2%
installation saving applies to archived rendering, packaging and delivery.
Full monitoring retains the same dependency versions through the `sec` extra.
Doctor reports missing SEC support, and scheduler installation blocks on it;
independent IR discovery still continues after a SEC failure.

Validation: 423 offline tests pass, plus 51 source/pipeline tests against the
clean installed full package outside the checkout. Both installed profiles
reproduce all 35 complete report/email surfaces and all 14 CLI files. The
checkout and legacy route also reproduce those baselines. All 21 report,
document and evidence metadata files match after excluding only the fixture
adapter's fresh retrieved/discovered timestamps for five Canadian packets.
All 60 current layout/contrast cases pass, and all 5,283 protected files retain
their hashes. Source freshness, financial formatting, publication holds and
delivery-isolation regressions remain covered.

Detailed measurements, comparisons, retirement evidence and independent review
are retained in `output/lightweight-refactor-20260905/`. Historical outputs and
the previous verification record above remain preserved. This refactor does
not authorize sending, deployment or schedule activation.
