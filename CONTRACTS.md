# Integration contracts

User specification: read `C:/Users/bkara/.codex/attachments/9d33e68e-92df-410e-82ba-d71f532e4890/pasted-text-1.txt` before work. Apply the project working instructions in `AGENTS.md` and `GOAL_PROMPT.txt`. User-level Codex working defaults are in `C:/Users/bkara/.codex/AGENTS.md`. Do not overwrite others' changes.

The revised objective in GOAL.md supersedes conflicting portions of the attached specification. Astra/root owns design leadership, information selection, reporting.py, precise UI instructions, visual acceptance, models.py, config.py, state.py, pipeline.py, cli.py, pyproject.toml and integration/docs. Luna workers own the bounded files assigned in their prompts, including HTML/plain-text template execution under Astra design direction when assigned. Coordinate public interfaces here before changing them.

* Configuration is a plain dictionary from TOML. `config['_root']` is absolute config-directory string, `config['_storage']` is absolute storage-directory string. `config['companies']` is a list of company dictionaries with ticker, name, cik, enabled, ir_url, ir_pages, etc. Settings: [sources], [email], [ai], [schedule], [storage]. Watchlist goes in the SAME config file.
* Source worker: `servicing_brief.sources.collect(config: dict, *, bootstrap: bool=False, checkpoints: dict|None=None) -> SourceResult`. Independent SEC/IR coverage. `doctor_sources(config)->list[dict]` optional. Own sources/, source tests, `config.example.toml` watchlist/settings, docs/SOURCES.md. Use Document and SourceResult from models.py. Original files beneath config['_storage']/archive. Do not run SEC concurrently outside shared acquisition guard. First focus TFC/PFSI, then WFC/RKT, then validate disabled candidates.
* Financial worker owns extraction.py, evidence.py, narrative.py and financial tests only. Evidence retains original source excerpts and exact Decimal strings. Read original archived documents; PyMuPDF is the sole PDF parser. Do not alter config.example.toml: message root with required settings.
* Astra reporting: `servicing_brief.reporting.build_report(config: dict, documents: list[Document], *, baseline: bool=False, coverage: dict|None=None, previous_documents: list[Document]|None=None) -> dict` accepts exactly one CIK/reporting event, rejects mixed CIKs, and returns `subject`, `html`, `text`, `evidence` (JSON-safe), one `company_reports` entry and supporting metadata. Prior-period evidence is restricted to the same reporting company. No consolidated cross-company output.
* Delivery worker: `servicing_brief.delivery.prepare_messages(config: dict, report: dict, documents: list[Document], output_dir: Path) -> list[Path]` writes .eml files without sending. `deliver_messages(config: dict, message_paths: list[Path], state_db: Path) -> list[dict]` persists independent message-level delivery acknowledgment. `send_test(config: dict, state_db: Path) -> list[dict]` may delegate those functions; CLI gates explicit --send. Own delivery.py, scheduling.py, scripts/, delivery/scheduling tests, docs/DELIVERY.md. Recipient unset = preview To header omitted, never invent one. Exact MIME budget. Use separate SQLite delivery tables in same DB. Main pipeline holds overall run lock; delivery should independently prevent duplicates/overlap. Do not live send or install tasks.
* Root pipeline groups events by CIK+period, retains every document and its versions, tracks collection separately from briefing and sends. Later 10-Q and revisions trigger updates. Dry-run keeps preview history but never marks provider acknowledgment. Failed/pending sources persisted. Sources may rediscover overlaps; persistent document ID/hash deduplication belongs in root.
* Tests run via `uv run pytest`; root owns dependency changes. Workers may ask to add deps, never create competing environments. No normal network tests. Workers return files changed, tests, findings, unresolved issues. Model explicitly Luna max via supported spawn controls.
